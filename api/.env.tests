# sync schema with DB - disable ORM_SYNC on staging and production
ORM_SYNC = true
# drop database - disable ORM_SYNC on staging and production
ORM_DROP_DB = true
ORM_URL = postgresql://aireal-tests:password@localhost:5433/aireal-tests

# Sentry
SENTRY_ENV = 
SENTRY_DSN = 

# no account on BO, just a token to be connected
PASS_KEY = test
JWT_KEY = test

ALERTS_URL = https://data.airpl.org/api/v1/alerte/description-alerte/?format=json