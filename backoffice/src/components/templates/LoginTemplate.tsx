import ARLoginLayout from "../organisms/ARLoginLayout";

const LoginTemplate = () => {
  return (
    <div
      style={{
        display: "flex",
        width: "100%",
        height: "100vh",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <ARLoginLayout />
    </div>
  );
};

export default LoginTemplate;
